apiVersion: apps/v1
kind: Deployment
metadata:
  name: streamlit
  namespace: ollama-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: streamlit
  template:
    metadata:
      labels:
        app: streamlit
    spec:
      containers:
        - name: streamlit
          image: orionpax77/streamlit-app:latest
          ports:
            - containerPort: 8501
          env:
            - name: OLLAMA_HOST
              value: "http://ollama:11434"
