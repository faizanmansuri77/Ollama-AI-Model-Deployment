apiVersion: v1
kind: Service
metadata:
  name: streamlit
  namespace: ollama-app
spec:
  selector:
    app: streamlit
  ports:
    - protocol: TCP
      port: 8501
      targetPort: 8501
      nodePort: 30007
  type: NodePort
